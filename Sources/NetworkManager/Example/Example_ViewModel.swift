//
//  Example_ViewModel.swift
//  
//
//  Created by Tushar Lama on 18/02/23.
//

/*
class ViewModel {
    private let netWorkObj: NetworkManagerObj
    var model: YourModelClass!
    
    init(netWorkObj: NetworkManagerObj) {
        self.netWorkObj = netWorkObj
    }
    
    func fetchData() {
        let request = NetworkRequestData()
        networkService.request(request) { [weak self] result in
            switch result {
            case .success(let response):
                self.model = response
            case .failure(let error):
                print(error.debugDescription)
            }
        }
    }
} */
